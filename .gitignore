# zsh
.zcompdump*

# neovim runtime state (not config)
nvim/plugin/
nvim/neoclip.sqlite3

# tmux plugin manager state (installed by tpm)
tmux/plugins/

# secrets / local env
.env
.env.local
secrets/env

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
